---
import Nav from "./Nav.astro";
import NavMobile from "./NavMobile.astro";
import SocialMedia from "./SocialMedia.astro";

export interface Props {
    color: string;
    mainPage:boolean;
}

const { color, mainPage } = Astro.props;

---

<header class="box-border font-mono m-0 w-screen z-10 fixed flex flex-row select-none top-0 pointer-events-none">

    <SocialMedia color={color}/>

    <Nav mainPage={mainPage}/>

    <NavMobile />

</header>
